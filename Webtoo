#!/usr/local/bin/perl6


BEGIN {
    @*INC.push: './lib';
}

use Webtoo;

my $datadir = './data';
my $config  = 'config';

my $wt = Webtoo.new( :datadir($datadir) );
$wt.loadMetadataFile($config); # The global config.
say $wt.processContent;

